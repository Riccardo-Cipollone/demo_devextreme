import { ColoredBadgeDirective } from './colored-badge.directive';

describe('ColoredBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoredBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
